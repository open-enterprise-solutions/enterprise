# Session 2026-05-10 — iterator refactor, shutdown deadlocks, CES IntelliSense, lambda-eval guards

Long arc on top of the lambda landing from the night before. Five
distinct pieces moved at once; everything Debug/x86 builds clean.
Working copy uncommitted at end of session per `feedback_no_autocommit`.

The pieces, in the order they happened:

1. Iterator subsystem refactor (Phases 1–5).
2. Shutdown deadlock fix (registry ↔ ThreadBody / debug listener).
3. IntelliSense gaps — anonymous functions, preprocessor directives,
   lambda-eval crash logging, CES syntax support.
4. Lambda eval-in-lambda crash diagnosed + hardened (real fix
   deferred).
5. CES flipped to default codeStyle.

## 1. Iterator subsystem — phases 1–5

The old surface had four virtuals per iterable: `HasIterator()`,
`GetIteratorEmpty()`, `GetIteratorAt(idx)`, `GetIteratorCount()`. The
runtime drove iteration through an inline `ibValueIterator` that held
`ibValue& m_ownerValue` and walked indices. Several latent bugs and
no path to native cursor-paginated DB iteration.

### Phase 1 — cursor API + Break-leak fix

New abstract base in `value.h`:

```cpp
class ibValueIteratorState {
public:
    virtual ~ibValueIteratorState() = default;
    virtual bool MoveNext(ibValue& current) = 0;
    virtual void Reset() = 0;
    virtual bool PeekSample(ibValue& current) const { return false; }
};
```

`ibValue::CreateIterator()` returns `std::shared_ptr<ibValueIteratorState>`.
Default delegates through TYPE_REFFER chain to the underlying object;
returns null when not iterable.

`ibValueIterator` runtime holder rewritten: holds the state plus a
`m_hotFromNextIter` flag. `OPER_NEXT_ITER` sets the flag, `OPER_FOREACH`
consumes it. Re-entry into FOREACH with the flag clear (Break-leak from
an outer-loop iteration) drops the stale state and rebuilds from
`variable2` — which is the just-LET'd `@in_` slot, so a re-evaluated
iterable picks up correctly.

Off-by-one fixes (`>` → `>=`) in four spots: `tableInfo.h`,
`valueTable.h`, `tabularSection.h`, `commonObject.h`.

Removed `s_defaultOwner` static and the legacy `GetCurrentValue /
NextIterator / ResetIterator` accessors.

### Phase 2 — RAM-paged filter+sort consistency

`ibValueModel::CreateIterator()` overrides return a paged-cursor state
(`ibValueModelPagedIteratorState` in `tableInfo.cpp`) that drives the
model's `GetFirstFetch / GetNextFetch` in 64-row batches. RAM-paged
children (TabularSection / valueTable / RecordSet) automatically
inherit filter+sort consistency with the GUI because their Get*Fetch
slices `BuildVisibleView`.

Detection: `Features::RamFetch | Features::DbFetch` set, `Tree` clear.
Tree models fall through to the base default (no flat-walk over a tree
— would surprise users).

### Phase 3 — DB-paged lists script-iterable

Catalog / Document / Register lists become script-iterable for free
through the same `ibValueModel::CreateIterator` path. `EnumRef` got a
missing `GetFeatures` override (DbFetch flag) so it slots into the
same machinery. No per-class iterator-state code.

### Phase 4 — legacy API cleanup

`HasIterator / GetIteratorAt / GetIteratorCount` removed from `ibValue`
base API and all overriders (~13 places). Each non-Model iterable
class (`ibValueArray`, `ibValueContainer/Structure`,
`ibValueModelColumnCollection`, three record-prop walkers in
`commonObject.h`, `ibValueForm::ibValueFormCollectionControl`,
`ibValueFrame::ibValueEventContainer`) got its own inline
`CreateIterator()` override walking its native storage directly. The
former `IndexedIteratorState` wrapper that bridged the old API to the
new one is gone — there's nothing left to bridge.

`RefreshTabularSection()` deleted (zero callers post-2026-05-07
`NotifyReset()` collapse).

### Phase 5 — PeekSample + shared_ptr

`GetIteratorEmpty()` was the only legacy survivor (used by IntelliSense
type-hint). Moved into the iterator state itself as
`PeekSample(ibValue&) const` — symmetric with `MoveNext`'s out-param
shape. IntelliSense's call (`codeEditorInterpreter.cpp:1265-area`) now
goes `state = paramObject.CreateIterator(); state->PeekSample(skel);`.

Model children's typed-skeleton factory moved from `GetIteratorEmpty`
to a fresh `ibValueModel::GetEmptyRow()` virtual; `ibValueModelPaged
IteratorState::PeekSample` calls it and treats `TYPE_EMPTY` as "no
hint".

`m_state` in `ibValueIterator` switched from raw `ibValueIteratorState*
+ delete in dtor` to `std::shared_ptr<ibValueIteratorState>` per
project convention (`feedback_prefer_shared_ptr`). All `CreateIterator`
overrides return shared_ptr / `make_shared`.

### Files

* `src/engine/backend/compiler/value.{h,cpp}` — abstract state +
  `CreateIterator`.
* `src/engine/backend/compiler/procUnit.cpp` — `ibValueIterator`
  rewrite, OPER_FOREACH / NEXT_ITER, default `ibValue::CreateIterator()`.
* `src/engine/backend/tableInfo.{h,cpp}` — `ibValueModel::CreateIterator`
  + `GetEmptyRow` + paged iterator state.
* `src/engine/backend/system/value/valueArray.h`, `valueMap.{h,cpp}`,
  `valueTable.h`.
* `src/engine/backend/metaCollection/partial/tabularSection/tabularSection.{h,cpp}`
  (also `RefreshTabularSection` removal).
* `src/engine/backend/metaCollection/partial/commonObject.h` — three
  record-prop walker overrides + RecordSet `GetEmptyRow`.
* `src/engine/backend/metaCollection/partial/list/objectList.h` —
  EnumRef `GetFeatures`.
* `src/engine/frontend/visualView/ctrl/form.{h,cpp}`,
  `frame.{h,cpp}`, `formControl.cpp`, `frameEvent.cpp`.
* `src/engine/frontend/win/editor/codeEditor/codeEditorInterpreter.cpp`
  — IntelliSense type-hint via state.

## 2. Shutdown deadlock — registry ↔ ThreadBody / debug listener

Diagnosed from a real hung-process dump via cdb. Two coupled hangs.

### Listener spin

`ibDebuggerServerConnection::EntryClient` (`debugServer.cpp:684`) cycles
through `wxSocketServer::WaitForAccept(0, waitDebuggerTimeout)` with
TestDestroy poll between ticks. `wxThread::Delete()` sets the destroy
flag; the loop exits within one tick. But on a crash path
`ibDebuggerServer::Destroy()` was never called, so the flag was never
set. The wxTHREAD_JOINABLE listener kept the process alive.

There is also an `Accept(true)` branch in the same loop (taken when
`m_waitConnection == true`) that blocks indefinitely without polling
TestDestroy. Even a correct Delete + Wait wouldn't break that one.

Fix in `ShutdownServer`: destroy `m_socketServer` before `thread->Wait()`.
WaitForAccept / Accept return immediately on a destroyed listener,
TestDestroy fires on the next iteration, the thread exits cleanly. The
worker's own dtor / OnKill paths null the same pointer; the duplicate
Destroy is a no-op.

### Registry / main thread mutex chain

Single backend `.join()` callsite — `ibSessionRegistry::Stop` joining
`m_thread` (the ThreadBody). The original Stop() submitted Remove
requests for every owned session before joining; ThreadBody picked them
up via `Submit + Drain` and ran `ProcessRemove` from the registry
thread. Each ProcessRemove fires `NotifyDisconnect` listeners,
`DetachRuntime` on the module manager, etc.

`BeforeStart` (`moduleManagerEvent.cpp:37`) takes a
`lock_guard<m_runtimeMutex>` for the ENTIRE `CallAsProc("beforeStart")`
duration. SEH dispatch does NOT unwind C++ frames before the filter
runs, so a script crash inside BeforeStart leaves the lock_guard held
while the SEH chain runs OnFatalException → DestroyAppDataEnv →
~ibApplicationData → registry.Stop() → m_thread.join(). ThreadBody is
mid-ProcessRemove → DetachRuntime → blocked on the same m_runtimeMutex.
Deadlock.

Two-step fix:

A. **Inline drain in Stop().** Set m_stop, notify, join — then drain
   Removes inline on main, NOT through ThreadBody. ThreadBody's queue
   stays empty, it sees m_stop and exits. Removes the cross-thread
   mutex chase entirely on the normal path.

B. **Timed join + detach backstop.** Even after (A), a Remove that was
   already in flight when Stop ran can leave ThreadBody mid-ProcessRemove.
   Stop now polls `m_threadAlive` with a 2-second deadline; on timeout
   it calls `m_thread.detach()` and skips the inline drain (which would
   itself deadlock for the same reason). Process exits, OS reaps the
   thread.

The detach branch logs a warning. If we ever see it in normal use
that's a real bug; on crash paths it's the expected escape hatch.

### Files

* `src/engine/backend/session/sessionRegistry.cpp` — `Stop()` rewrite.
* `src/engine/backend/debugger/debugServer.cpp` — `ShutdownServer`
  destroys socket before Wait.

## 3. IntelliSense — four small items

### Item 1: anonymous functions in expressions

`var f = Function(x) ... EndFunction;` and the CES counterpart
`var f = Function(x) { ... };` weren't expressions to the IntelliSense
parser — `GetExpression` had no KEY_FUNCTION/KEY_PROCEDURE branch.

Added a depth-tracked skip that swallows the signature `(...)` and the
body, returning a typed-empty `FUNCTION` variable. Body parsing is
deliberately skipped (a full pass would need a child `m_activeContext`
push/pop); a future iteration can expose lambda locals to autocomplete.

### Item 2: preprocessor directives

`#define X`, `#region X`, `#ifdef X` etc. — the lexer was dropping the
keyword via `continue` in both PrepareLexem variants but keeping the
following identifier as a free token. The parser then tried to interpret
that ident as an expression and fell over.

Fix: when a directive keyword is seen, record `directiveLine =
m_currentLine` and drop every subsequent token while we're still on
that line. Applied at all four emit sites (full + incremental tokenizer,
keyword-path + CONSTANT-path each).

### Item 3: lambda eval crash logging

User reported a silent process death when evaluating an expression in
the debugger watch panel from inside a paused lambda. Hard SEH; the
shutdown deadlock fix from §2 made the death stop hanging the process,
which was a prerequisite for collecting a clean dump.

Added `lambda.log` file-based logging in CWD. `wxLogDebug` alone
routes only to OutputDebugString; users without DebugView never saw
the messages. The new helper writes a timestamped line per call:

```
[09:14:18.123] [lambda] OPER_LFUNC materialise: bc=0x… funcIdx=N endIp=M lambdaName=<lambda@N>
[09:14:18.124] [lambda] OPER_CALL_VAL: <lambda@N> callerArgs=K lambdaParams=M lambdaVars=V
[09:14:18.125] [lambda] Execute begin: lambda=<lambda@N> parentBc=0x… …
[09:14:18.125] [lambda] body ip=3 op=33 p1=(a=0,i=0) p2=(a=-1,i=-1) p3=(a=0,i=0) p4=(a=0,i=0)
[09:14:18.125] [lambda] body ip=4 op=1  p1=(a=-3,i=1) p2=(a=1000,i=1) …
[09:14:18.125] [lambda] body ip=5 op=55 p1=(a=-3,i=2) … method='Message'
[09:14:18.140] [lambda] dispatch ip=3 op=33
[09:14:18.140] [lambda] dispatch ip=4 op=1
   ← (no further entries: process died at ip=5)
```

Per-opcode dispatch trace is gated on
`pContext->m_currentFunction->IsLambda()` so the log doesn't get
flooded by named-function execution.

### Item 4: CES syntax support

The IntelliSense's `CompileBlock` and statement parsers (`CompileIf` /
`CompileWhile` / `CompileFor` / `CompileForeach` / `CompileException`
/ `CompileFunction`) were VBS-only. CES code in the editor broke
autocomplete past the first control structure.

Mirrored the backend's `ibCompileCode`'s CES patterns:

| Construct | VBS | CES |
|---|---|---|
| Block | keyword-fenced (EndIf / EndDo / EndFunction / …) | `{ … }` |
| `if` | `If c Then … EndIf` | `if (c) { … }` or `if (c) stmt;` |
| `while` | `While c Do … EndDo` | `while (c) { … }` |
| `for` | `For i = 1 To N Do … EndDo` | `for (i = 1 To N) { … }` |
| `Foreach` | `Foreach x In c Do … EndDo` | `Foreach (x In c) { … }` |
| `try` | `Try … Except … EndTry` | `try { … } except { … }` |
| `function` | `Function f(…) … EndFunction` | `Function f(…) { … }` |
| inline lambda | `Function(x) … EndFunction` | `Function(x) { … }` |
| `;` | terminates a block | statement separator |

Brace-less single-statement form `if (cond) Message();` was an
afterthought — the backend uses `m_numReturn == RETURN_BLOCK` to know
"single statement only"; IntelliSense doesn't have that context kind,
so a local `singleStmtMode + parsedOne` flag pair handles it. Set
`parsedOne = true` after each completed statement; if singleStmtMode
exit on the next loop turn.

`KEY_RETURN` correctly handles `;` at expression-required position;
the existing `if (IsNextDelimeter(';')) return false;` was already
right for both styles (CES uses `;` to end the statement).

The mode is read from `ibCompileCode::GetCodeStyle()` — process-global
flag set by metadata loader / codeRunner UI dropdown.

### Files

* `src/engine/frontend/win/editor/codeEditor/codeEditorInterpreter.cpp`
  — all four items.
* `src/engine/backend/compiler/procUnit.cpp` — lambda diagnostic logs
  (file-based `LambdaLog` helper near top-of-file, anon namespace).

## 4. Lambda eval-in-lambda — root cause + temporary guard

Diagnosed via the new logs + a fresh dump.

The crash chain is:

1. Lambda body executes (CALL_VAL → ibValueFunction::Execute → runtime
   ProcUnit Execute with `m_pByteCode = m_parentBc`).
2. Breakpoint fires; `EnterDebugger` → `DoDebugLoop` → user types
   `Catalogs.X` in watch.
3. `SendExpressions` → `ibProcUnit::Evaluate` → compiles a fresh eval
   bytecode via `ibCompileEval` against the current run context (the
   lambda's frame).
4. Eval ProcUnit's `m_pppArrayList` is laid out as
   `[0=own, 1=host=lambda, 2=host's-parent-module, …]` per
   `Evaluate`'s comment. For a lambda host, depth ≥ 2 is null —
   lambdas don't carry a parent-module-frame in pppArrayList; their
   parent walk lives on the bytecode side via `m_parent`.
5. Eval bytecode's `Catalogs` resolution emits a slot in the depth=2+
   range. ResolveWrite/ResolveRead deref `pppArrayList[depth][idx]`
   with `pppArrayList[depth] == nullptr`. Hard SEH.

### Temporary guard

`ResolveWrite` / `ResolveRead` got null-and-bounds checks (depth ≥ 0,
`pppArrayList != nullptr`, `pppArrayList[depth] != nullptr`,
`pppArrayList[depth][idx] != nullptr`). On any miss, throw
`ibBackendException("Outer frame not bound at depth N / idx K (eval
inside lambda?)")`. Watch panel surfaces a readable error; lambda
keeps running.

### Real fix — open

The eval ProcUnit needs to wire its `pppArrayList` to the lambda
runtime's chain instead of the host-frame chain. Two approaches sketched
in chat:

A. At `Evaluate` entry, detect "host is lambda" and substitute the
   pppArrayList with `[own, host=lambda, root_mm_frame_1,
   root_mm_frame_2, …]` (the runtime's chain that a normal lambda
   body opcode walks).
B. Compile eval bytecode with scope = lambda's parentBc; eval ProcUnit
   pulls the runtime's pppArrayList directly.

Either path is ~50–100 lines in the eval machinery. Deferred — the
guard is enough to keep the watch panel from killing the process, and
the SEH-via-shutdown problem is fixed independently.

## 5. CES is now the default

Process-global `gs_codeStyle` initial value flipped from `CODE_VBS`
to `CODE_CES`. New configuration metadata's Syntax property default
flipped from `syntax_vbs` to `syntax_ces`.

Naming clarified by the user:

* **CES** — c-style ES (Enterprise Solutions). C-flavoured: paren
  conditions, brace bodies, `;` terminators.
* **VBS / VES** — Visual Basic-style ES. Same family as VBScript,
  Visual Basic 6, and 1C:Enterprise's BSL — keyword-fenced control
  flow (Then / Do / EndIf / EndDo / End-…).

VBS is kept for legacy / VBS-style configurations and now serves as
a "please migrate" signal in the metadata UI rather than the default
authoring mode.

### Files

* `src/engine/backend/compiler/compileCode.cpp` — gs_codeStyle init.
* `src/engine/backend/metaCollection/metaObjectMetadata.h` —
  `m_propertySyntax` default.

## Build state

Debug/x86 builds clean across all the changes. Smoke-tested through
`launcher.exe`. No new warnings traceable to the iterator refactor /
shutdown fixes / IntelliSense work.

## Pending / open

1. **Lambda eval-in-lambda real fix.** Guard converts SEH to
   ibBackendException; eval still can't actually resolve Catalogs from
   inside a lambda. See §4 for the two approach sketches.
2. **`Accept(true)` in debug listener.** Not a problem with the new
   socket-destroy-before-Wait fix in ShutdownServer, but the branch is
   still infinite-blocking inside the wxThread body. Replace with
   `WaitForAccept(0, 50ms)` + TestDestroy poll for symmetry.
3. **Watchdog backstop on shutdown.** 2-second timed-join in
   sessionRegistry::Stop is the only timeout in the shutdown path.
   A top-level `_exit(0)` after 10s of teardown would guarantee no
   zombie process even on unforeseen mutex chains.
4. **Helper extract for CES gates in CompileBlock callers.**
   `if (IsCES()) ExpectDelimeter('('); else ExpectKeyword(KEY_THEN);`
   is replicated across four sites. `ExpectBlockOpener / ExpectBlockCloser`
   helpers would deduplicate. Cosmetic.
5. **VBS → CES converter.** Mentioned as a possible follow-on. Not
   started.
6. **Working copy uncommitted.** Spans this session + the lambda /
   paging arc from earlier in the week. Bisect difficulty growing;
   commit grouping pending.

## Decisions to remember

* **CES is the new default authoring mode.** Existing modules / configs
  preserve their stored Syntax property (so VBS-marked modules still
  parse VBS). Brand-new configurations land in CES.
* **Iterator API single source of truth: `CreateIterator()`.** Returns
  shared_ptr to a state with MoveNext / Reset / PeekSample. The legacy
  random-access surface (At/Count/Has/Empty) is gone from `ibValue`.
* **`GetEmptyRow()` on `ibValueModel`** — typed-skeleton factory used
  by the paged iterator's PeekSample. `TYPE_EMPTY` return = no
  IntelliSense hint; only TabularSection / valueTable / RecordSet
  override with real ReturnLine factories.
* **Lambda body always runs on the session's lambda runtime ProcUnit
  with `m_pByteCode` swapped to `m_parentBc`.** `ibValueFunction::Execute`
  guards with try/catch around the runtime->Execute call; on exception
  it logs the lambda name and `pContext->m_lCurLine` (last-attempted
  ip) before rethrowing.
* **Stop's job is to drop the queue, not to drain it.** The original
  shape (Submit Remove, then join) coupled main's shutdown to ThreadBody's
  in-flight work. Inline drain on main + timed-join + detach backstop
  is the canonical shape now.
* **Preprocessor directives consume the whole line in the IntelliSense
  lexer.** Same treatment as the comment-strip rule from earlier
  sessions — drop everything from the directive keyword to the next
  newline.

---

## Addendum — PM session (2026-05-10, continuation)

Long arc on top of the morning lambda + iterator landing. Three
clusters: (1) CES auto-indent and brace handling in the code editor,
(2) lambda eval real fix on the runtime side, (3) IntelliSense
recovery for CES module body + lambda bodies. Working copy still
uncommitted (now ~6 days span).

Build clean Debug/x86 throughout. `frontend.dll` was the only
churning binary — close `enterprise.exe` / `designer.exe` /
`launcher.exe` to relink.

### 1. CES auto-indent in code editor

Soft single-statement `if (cond) stmt;` form (CES) was tracked
nowhere — fold parser only marks `{...}` braces, so body line of a
brace-less header had no fold flag and `PrepareTABs` stripped its
`\t` on Enter. Five rounds of fixes landed:

* `IsCESSingleStmtHeader(line)` detector — line starts with
  `if`/`while`/`for`, ends with `)`, no `{`, comment-tail trimmed.
* `softBodyExtra = 1` when prev (skipping blanks) is a soft header —
  added to every branch's `applyIndent` target so body indent is
  preserved on Enter.
* `currIsSoftHeader → foldLevel += 1 + softBodyExtra` for new-line
  indent; `currLineBlank && prevIsSoftHeader → foldLevel +=
  softBodyExtra` so an empty `\t|` line doesn't drop back to col 0
  on Enter (still inside the soft body region pre-body).
* CES `applyIndent` is **no-shrink** — fold parser's foldLevel is
  often less than user's manual indent (no fold between soft scopes)
  so target=foldLevel would strip; only fill the deficit, never
  trim. VBS keeps strict-match (its fold vector is keyword-fenced
  and accurate).
* `IsLineJustOpenBrace(line)` + `currLineIsBraceOpener` — Allman
  `{` on its own line "consumes" the soft-body bump. Without this
  the `{` would land at body indent (one tab too deep) on Enter.

`OnCharAdded` handler bound to `wxEVT_STC_CHARADDED`:

* **`}` typed on a whitespace-only line** → walk back depth-tracked
  through raw bytes (wxSTC's `BraceMatch` returns -1 because our
  custom styler doesn't tag braces), find matching `{`, set line
  indentation to opener's. Auto-aligns Allman closers.
* **`{` typed on a whitespace-only line** (CES only) → walk back
  past blanks looking for a soft header; if found, dedent current
  line to header's indent. Allman dedent under `if (cond) \n {`.

### 2. Brace highlight + gutter cosmetics

Brace highlight via `wxEVT_STC_UPDATEUI` — manual matcher (same
brace-balance walk as `}` align), supports `{}` `()` `[]`. Style:
red foreground only, no bold or background — font metrics stay
stable while caret moves over braces.

`SetFontColorSettings` order fix: `StyleSetFont(STYLE_DEFAULT, font)`
+ fg/bg now happen **before** `StyleClearAll()` so the cascade
propagates the user's font (and point size) to every style index.
Without this, untouched styles (annotations, indent guides, callTip,
control chars, BRACELIGHT/BRACEBAD) kept the OS-default font and
zoom +1pt was applied to a different base — visually choppy /
non-proportional zoom.

Gutter gradient: line-number margin darkest, breakpoint medium,
fold lightest. `wxSTC_STYLE_LINENUMBER` bg = `#E8E8E8`, digits
`#A0A0A0`. `SetMarginBackground(BREAKPOINT, #F0F0F0)`,
`SetMarginBackground(FOLDING, #F6F6F6)`,
`SetFoldMarginColour(true, #F6F6F6)` + Hi colour. Margins read
as one continuous band fading into the white text area.

`MarginBackground` calls go through `SetMarginBackground(margin,
color)` (wxSTC API) — earlier `StyleSetBackground(margin_id, ...)`
on the breakpoint margin was a bug (margin id treated as style
index — set style 1 = wxSTC_C_DEFAULT to white).

BRACELIGHT/BRACEBAD foreground reapplied at the end of
`SetFontColorSettings` (after `StyleClearAll`) — otherwise the
ctor-time setup wiped on every settings refresh.

### 3. Lambda eval real fix — `m_pppArrayList` splice

Morning's guard converted SEH into ibBackendException. Real fix
landed in PM:

`ibProcUnit::CompileExpression` else branch (non-eval-of-eval host):
when `pRunContext->m_currentFunction->IsLambda()`, splice the
session's lambda shim's `m_pppArrayList[1..]` into eval's
`m_pppArrayList[2..]`. Eval has one extra own-frame layer above
shim (eval → shim → rootPu → …), so offset is `+1`.

```cpp
if (pRunContext->m_currentFunction != nullptr
    && pRunContext->m_currentFunction->IsLambda())
{
    ibProcUnit* const shim = pRunContext->m_procUnit;
    if (shim != nullptr && shim->m_pppArrayList != nullptr) {
        const unsigned int shimSize = shim->GetParentCount() + 2;
        const unsigned int evalSize = GetParentCount() + 2;
        for (unsigned int k = 2; k < evalSize; ++k) {
            const unsigned int srcIdx = k - 1;
            if (srcIdx < shimSize)
                m_pppArrayList[k] = shim->m_pppArrayList[srcIdx];
        }
    }
}
```

**Root cause**: lambda shim is a dispatcher, its `m_cCurContext` is
empty (frames live directly in `m_pppArrayList`, set by
`ibSession::CompileRoot`). The outer `Execute` populates eval's
`[k+2]` from `parent->m_cCurContext.m_pRefLocVars` — for the shim
that's null, hence depth ≥ 2 lands in null and SEH'd before the
guard.

After splice: depth 2 in eval → shim's `[1]` (rootPu frame), depth
3 → shim's `[2]` (= rootPu, duplicate but correct), depth 4+ →
common-module / parent frames. Catalogs / Documents / Manager
resolve through eval as expected.

Guard in `ResolveWrite/Read` stays as defensive — if splice ever
fails or shim doesn't supply something, watch panel shows readable
error instead of segfault.

### 4. IntelliSense in CES + lambda body

Reported one after another: `CompileModule` returned false on every
keystroke, autocomplete dead.

Three independent fixes landed:

**(a) `CompileDeclaration` consumes initializer.** CES `var x =
Catalogs.Items;` left the chain expression in the lexem stream;
outer `CompileBlock` then read it as a fresh statement and failed
on the `.` (no leading identifier state). Now calls `GetExpression()`
after `=` and captures inferred type into `AddVariable`. VBS uses
`Var x;` (originally spelled `Перем x;` in 1C-style sources) without
`=` so the path doesn't fire there.

**(b) `CompileBlock(bool allowSingleStmt = true)`.** Module-level
body in CES has no `{` opener but is multi-statement. The original
heuristic `if (IsCES() && !'{') singleStmtMode = true` was wrong
for module body — after the first `;` the block exited, leaving
the rest as trailing tokens, `cursor+1 < size-1 → return false`.
`CompileModule` now calls `CompileBlock(/*allowSingleStmt=*/false)`.
Control-statement callsites (`CompileIf` / `CompileWhile` /
`CompileFor` / lambda body) keep the default `true` for brace-less
single-stmt support.

**(c) Inline-lambda in `GetExpression`.** Was a depth-tracked body
skip → autocomplete inside lambda body got the enclosing function's
scope (or nothing). Now pushes a child `ibPrecompileContext` with
parent = root, registers params via `AddVariable`, calls
`CompileBlock` to walk the body, sets `m_cursorContext =
m_activeContext` if caret falls in, restores `prevActive` on exit.
Catalogs / Manager / Documents now visible via parent = root chain;
lambda's params + locals visible via the pushed context. Mirrors
runtime discipline (no enclosing-function scope leak).

### 5. Diagnostics still in tree

* `fold.log` — `PrepareTABs` per-Enter trace + Tab/BackTab/Format.
  In `codeEditorLoader.cpp` anon namespace. Live.
* `intelli.log` — `CompileModule` / `CompileBlock` / `GetCurrentIdentifier`
  trace. In `codeEditorInterpreter.cpp` anon namespace. Live.

Both write UTF-8 via `vsnprintf` (not `wxString::Format` — `%s`
would treat `char*` as `wchar_t*` on Windows and garble output).

### Files touched today (PM)

* `src/engine/frontend/win/editor/codeEditor/codeEditor.{h,cpp}` —
  brace styles in ctor + `OnUpdateUI` hook; OnCharAdded declaration.
* `src/engine/frontend/win/editor/codeEditor/codeEditorLoader.cpp` —
  `IsLineJustOpenBrace`, soft-body walkback, no-shrink CES applyIndent,
  HEADER-branch carry, OnCharAdded `{`/`}` aligners, OnUpdateUI brace
  highlight, FoldLog diagnostics, gutter gradient.
* `src/engine/frontend/win/editor/codeEditor/codeEditorInterpreter.{h,cpp}` —
  IntelliLog + LexTypeName helpers; CompileBlock takes `allowSingleStmt`;
  CompileDeclaration consumes initializer; inline-lambda push/parse/pop;
  CompileModule diagnostics. Logs in CompileBlock entry/iter/exit and
  GetCurrentIdentifier teleport + chain capture.
* `src/engine/backend/compiler/procUnit.cpp` —
  `CompileExpression`'s else branch splices lambda-shim chain into
  eval's `m_pppArrayList[2..]`.

### Decisions to remember (PM)

* **CES = no-shrink indent rewrite.** Fold parser only tracks
  braces; manual indent in soft-body regions or hand-aligned closers
  must be preserved. VBS stays strict-match.
* **`{` consumes soft-body bump (Allman).** When a CES single-stmt
  header is followed by a brace-only line, the `{` lands at header
  indent — body inside `{...}` is at +1 over the brace. `OnCharAdded
  '{'` enforces this on type, `IsLineJustOpenBrace` keeps PrepareTABs
  from undoing it.
* **`}` align uses manual brace-walk, not `BraceMatch`.** wxSTC's
  built-in needs lexer-side brace styling; our custom styler doesn't
  tag braces.
* **Lambda shim's frame chain lives in `m_pppArrayList` directly,
  not in `m_cCurContext`.** Set by `ibSession::CompileRoot` once
  per session. Eval inside lambda must splice this chain into its
  own array — the default `Execute` setup walks parents'
  `m_cCurContext` which is empty for the shim.
* **`CompileDeclaration` consumes the initializer expression.** CES
  `var x = expr;` requires it; VBS's `Var x;` doesn't have one.
  Inferred type carries through to `AddVariable` so `x.` chain
  autocomplete works.
* **Module body uses `allowSingleStmt = false`.** No `{` opener but
  not single-stmt. Only control-structure callsites use the default.
* **`StyleSetFont(STYLE_DEFAULT, ...)` BEFORE `StyleClearAll`.** The
  cascade propagates STYLE_DEFAULT to every style index so untouched
  styles (annotations, indent guides, BRACELIGHT/BRACEBAD) start
  with the user's font — proportional zoom requires same base size.

### Pending into next session

1. **Autocomplete-before-existing-code symptom.** User reports
   autocomplete doesn't trigger when there's code after the caret
   line. Logs show `CompileModule: OK` for current scenarios, but
   the user's specific repro hasn't been captured yet. Extra
   diagnostics in CompileBlock / GetCurrentIdentifier landed —
   awaiting fresh `intelli.log`.
2. **Lambda + AOT cache verification.** Still untested.
3. **Diagnostic logs cleanup.** `fold.log` + `intelli.log` to be
   removed once IntelliSense paths stabilise.
4. **Commit grouping.** Working copy spans 6 days. Bisect ineffective.
5. **`Accept(true)` debug listener** + **watchdog `_exit(0)`**
   carryover from morning. Still open.

---

## Addendum — late-PM (2026-05-10, third pass)

Bug pass on the morning + PM IntelliSense work, plus the VBS → VES
rename and the diagnostic-log cleanup that the previous addendum
deferred. Build clean Debug/Win32, working copy still uncommitted.

### 1. IntelliSense — three follow-up fixes

**(a) Function body parsed as single-statement.** `CompileFunction`
called `CompileBlock(/*default*/)` after consuming the param list,
so a function body fell into single-statement mode and exited after
one `;`. Effect: any inline lambda inside a named function leaked
its `EndFunction` to the top-level parser → `CompileModule` failed
on every keystroke past the first such function. Fixed by passing
`/*allowSingleStmt=*/false` in `CompileFunction` and in the lambda
branch of `GetExpression`.

**(b) `m_declPos` plumbed through implicit-decl.** `var` is rare in
script code; the typical pattern is `a = expr;` and the LHS
auto-declares `a`. Without a declaration position the autocomplete
saw `a` everywhere — including lines above the implicit decl, which
was the user-visible "shows variable above its declaration" bug.

`ibPrecompileVariable::m_declPos` records the source-text offset of
the (auto-)declaring reference; the LoadAutoComplete walker filters
out variables whose `m_declPos > caretPos`. Parameters and context
injections keep `m_declPos = 0` so they're always visible.

Wired through:
* `ibPrecompileContext::AddVariable` and `GetVariable` accept
  `int declPos = 0`.
* `ibPrecompileCode::GetVariable(name, checkError, declPos)` wrapper.
* `GetCurrentIdentifier` SET branch passes `nStartPos` so the
  position points at the LHS occurrence (the de-facto declaration).

**(c) Empty dropdown at file top.** `PrepareExpression` greedily
captures the full identifier under the caret, so at column 0 of an
empty/early line `currentWord` came out non-empty. The list filter
then matched nothing and the dropdown showed empty. Fix:
`if (lenEntered == 0) currentWord = wxEmptyString;` in
`StartAutoComplete`. The user types forward to filter live; nothing
to match against at start-of-word means show all candidates.

### 2. Fold parser — unify CES + VES

The PM addendum's CES soft-statement work flipped the fold parser
into a brace-only mode for CES, dropping the keyword fold path.
Effect: switching the syntax-mode dropdown live (codeRunner) made
all `Procedure ... EndProcedure` / `If ... EndIf` folds vanish on
legacy keyword-style sources.

Rewrote `ibFoldLevelParser::CalcFoldLevel`: both modes share the
keyword-fold openers (Procedure / Function / If / While / For /
Try) and matching `End*` closers. CES adds `{ ... }` brace folds on
top — a hybrid `Procedure foo() { … } EndProcedure` folds at both
levels and the trailing-unclosed-fold trim in `UpdateFoldLevel`
prunes the keyword leg if the closer is missing in pure CES style.

### 3. VBS → VES rename

User clarification: VES = "Visual Basic-style ES + BSL-style mix". The
old VBS label was conflating two things; VES is the user-visible
name going forward, VBS stays only as the wire token in
`metaObjectMetadataEnum.h` for back-compat with stored
configurations.

Renamed:
* enum: `syntax_vbs` → `syntax_ves` (`backend_core.h`).
* code-style: `CODE_VBS` → `CODE_VES` (`compileContext.h`,
  12× `compileCode.cpp`).
* codeRunner UI dropdown label "VBS" → "VES".
* All comments / doc references in `codeEditor.h`,
  `codeEditorInterpreter.cpp`, `metaObjectMetadata.h`.

Wire token kept as `wxT("vbs")` with a comment explaining why; user-
facing label is `_("ves")`. Existing serialised configs continue to
load.

### 4. `MakeProcedureTemplate` helper

Two callsites generate `Procedure name(args) ... End`/`{...}`
templates: the visual-editor's "create handler from event" path
(`visualEditorNotebook.cpp:ModifyEvent`) and the procedures-and-
functions search dialog (`functionSearcher.cpp`). Both were stamping
VBS-only `Procedure ... EndProcedure` regardless of the active mode.

Static helper `ibCodeEditor::MakeProcedureTemplate(name, args)` reads
`ibCompileCode::GetCodeStyle()` and emits the matching template (CES
braces vs VES keyword fences). Both callsites switched to the helper.

### 5. Diagnostic-log cleanup

Removed all three log helpers and their callsites:

* `LambdaLog` / `lambda.log` — `procUnit.cpp` anonymous namespace
  helper, ~25 callsites in `ibValueFunction::Execute`, dispatch
  loop, `OPER_LFUNC`, `OPER_CALL_VAL`. Per-opcode body dump and the
  full body-opcode pre-dump also removed (diagnostic-only). Headers
  `<wx/ffile.h>`, `<wx/datetime.h>`, `<mutex>` removed from
  `procUnit.cpp` (no other consumers).
* `IntelliLog` / `intelli.log` — `codeEditorInterpreter.cpp`
  anonymous namespace + `LexTypeName` helper. Callsites in
  `CompileModule`, `CompileBlock`, `GetCurrentIdentifier`. Diagnostic
  includes (`<wx/file.h>`, `<wx/datetime.h>`, `<cstdarg>`,
  `<cstdio>`, `<cstring>`) removed.
* `FoldLog` / `fold.log` — `codeEditorLoader.cpp` anonymous
  namespace + `EscapeWS`, `FoldFlagsName` helpers. Callsites in
  `PrepareTABs`, `FormatSelection`, `OnCharAdded`, `IncreaseIndent`,
  `DecreaseIndent`. Same diagnostic includes removed.

Also fixed two pre-existing `const const ibValueMetaObjectModuleBase*`
warnings in `codeEditorLoader.cpp` (left over from the const-meta
refactor).

### Files touched (third pass)

* `src/engine/backend/backend_core.h` — `syntax_vbs` → `syntax_ves`.
* `src/engine/backend/compiler/compileContext.h`,
  `compileCode.cpp` — `CODE_VBS` → `CODE_VES`.
* `src/engine/backend/compiler/procUnit.cpp` — LambdaLog removal,
  diagnostic include cleanup.
* `src/engine/backend/metaCollection/metaObjectMetadata.h`,
  `metaObjectMetadataEnum.h` — comments + wire-token comment.
* `src/engine/codeRunner/codeRunner.cpp` — `CODE_VES`, dropdown
  label.
* `src/engine/frontend/win/editor/codeEditor/codeEditor.{h,cpp}` —
  fold parser unification, `MakeProcedureTemplate` static helper.
* `src/engine/frontend/win/editor/codeEditor/codeEditorInterpreter.{h,cpp}` —
  `m_declPos` field + plumbing, IntelliLog removal, function-body
  parse-mode fix.
* `src/engine/frontend/win/editor/codeEditor/codeEditorInterpreterContext.cpp` —
  declPos in AddVariable / GetVariable.
* `src/engine/frontend/win/editor/codeEditor/codeEditorLoader.cpp` —
  declPos filter, empty-currentWord override, FoldLog removal,
  const-const warnings.
* `src/engine/frontend/win/editor/codeEditor/components/autoComplete.h` —
  reverted log-only accessors.
* `src/engine/frontend/win/dlgs/functionSearcher/functionSearcher.cpp`,
  `src/engine/designer/win/editor/visualEditor/visualEditorNotebook.cpp` —
  switch to `MakeProcedureTemplate`.

### Decisions to remember (third pass)

* **VES = user-visible label.** Wire token stays `vbs` in serialised
  metadata for back-compat; never rename across stored configs.
* **`m_declPos = 0` means always-visible.** Parameters and context-
  injected names keep zero; only in-body decls (explicit `var x`
  or implicit `x = expr` first reference) carry a non-zero offset.
* **Function body uses `allowSingleStmt = false`.** Same rule as
  module body — no `{` opener but multi-statement. Single-stmt
  mode is for inline `if (cond) stmt;` only.
* **Both syntax modes share keyword fold parsing.** CES adds brace
  folds on top, never replaces. Hybrid sources fold cleanly at both
  levels.
* **`MakeProcedureTemplate(name, args)` is the only emitter.** Both
  callsites (visual editor event handler + search dialog) route
  through it; mode is read once from `ibCompileCode::GetCodeStyle()`.

### Pending into next session

1. **Lambda + AOT cache verification** (carry-over).
2. **`Accept(true)` debug listener** + **watchdog `_exit(0)`**
   (carry-over).
3. **Helper extract for CES gates** (`ExpectBlockOpener` /
   `ExpectBlockCloser`) — cosmetic, carry-over.
4. **VES → CES converter** — not started, carry-over.
5. **Commit grouping.** Pending user permission for commit pipeline.

---

## Addendum — fourth pass (2026-05-10, evening)

CES bring-up continued: block scope landed end-to-end (compile +
runtime + autocomplete + highlighter), six tab/indent bugs closed,
fold parser cleaned for CES, callable-vars from nested block scope
fixed, file-based diagnostic log infrastructure (`scope.log` /
`fold.log` / `tab.log`) put in place. Build clean Debug/Win32. Working
copy still uncommitted.

### 1. CES block scope `{ }` — full path

`compileContext.h` got a sixth context kind:

```cpp
enum {
    RETURN_NONE = 0,
    RETURN_PROCEDURE,
    RETURN_FUNCTION,
    RETURN_LAMBDA_PROCEDURE,
    RETURN_LAMBDA_FUNCTION,
    RETURN_BLOCK,              // block-scope (`{ }` in CES, control body)
};
```

`CompileBlock` (`compileCode.cpp`) recognises bare `{` inside any
running block — the CES branch handles `bare {` by recursing with a
new local ctx (parent = current), via `CreateLocalContext`, compiling
the body recursively, then consuming `}`. A var declared in a block
scope routes through `ibCompileContext::AddVariable`'s RETURN_BLOCK
branch — physical slot allocated in the host frame via `CreateVariable`
chain (`@context%d` temps), but the entry lives in the child ctx's
`m_listVariable` so it disappears when the block closes (visibility
guarantee). Compile-time emits `OPER_CTX_BEGIN` / `OPER_CTX_END`
declarators; runtime treats both as NOP (block scope is compile-only,
slots stay in host frame).

**Module-level boot-loop fix.** `CompileBlock`'s ENTER had:

```cpp
if (gs_codeStyle == CODE_CES && (... != RETURN_NONE && ... != RETURN_BLOCK) || IsNextDelimeter('{')) {
    GETDelimeter('{');
    ...
}
```

Operator precedence trap — `&&` binds tighter than `||`, so the
expression parsed as `(CES && X) || IsNextDelimeter('{')`. Module body
(RETURN_NONE) saw a leading `{`, ate it, then looped forever in the
`}` branch (RETURN_NONE didn't break and `m_numCurrentCompile--` re-fed
the same `}`). Fixed by tightening to:

```cpp
if (gs_codeStyle == CODE_CES && context->m_numReturn != RETURN_NONE && IsNextDelimeter('{'))
```

`}` at module level is now an explicit `SetError(ERROR_CODE)` instead
of silent infinite loop.

### 2. Block-scope vars callable from nested blocks

`var func = Function(){...}` declared in block-A, then `func()` called
from block-B nested inside block-A — failed with `Procedure or function
not detected (func)`. Two cascading bugs:

* `GetCurrentIdentifier`'s **callable-var branch** at `compileCode.cpp:2187`
  used `context->FindVariable(...)` which is **own-scope only** (no
  parent walk). Nested block scope didn't see the outer block's var.
  Replaced with explicit `for (pCur = context; pCur; pCur = pCur->m_parentContext)`
  loop — first match wins.

* Even when found, the gate `!foundedCallableVar->m_bContext` rejected
  it. `ibCompileContext::AddVariable`'s RETURN_BLOCK branch artificially
  flags every block-scope var with `contextVar=true` (so the runtime
  emission path treats them as outer-frame slots, same routing as real
  Context bindings like `Manager` / `ThisForm`). The gate now
  distinguishes: `bContext=true` is allowed when the var was found in
  a `RETURN_BLOCK` ctx (block-scope artifact) — only real root-context
  bindings (numReturn==RETURN_NONE on `m_rootContext`) get filtered.

Both were diagnosed via the new `scope.log` — full trace of
AddVariable / FindVariable / callable-var decision on every compile.

### 3. Tab / auto-indent: four cascading bugs

User repro: simple `{ ... }` block, every other Enter inside the
block dropped a stray space; typing `{{{` corrupted the buffer with
~2KB of tabs.

**Bug 3a — `GetFoldMask` operator-precedence trap.** Body of the
fold-mask getter (codeEditor.h):

```cpp
return wxSTC_FOLDLEVELBASE_FLAG + (level - 1) | flag;
```

`+` binds tighter than `|`, so the expression evaluated as
`(BASE + level - 1) | flag`. With `level == 0` (mid-typing of unbalanced
braces), result = `(0x400 - 1) | 0x2000 = 0x23FF`. Caller stripped the
flags and got `baseLevel = 2047`, then `applyIndent(2047)` faithfully
emitted 2047 tabs into the line. Buffer corrupted, brace-walk broke.
Fixed with parens + `std::max(level - 1, 0)` clamp on the HEADER branch.

**Bug 3b — `applyIndent` no clamp.** Defence-in-depth — even if
`GetFoldMask` returns a stale or absurd level, `applyIndent` now
clamps `targetTabs ∈ [0, 32]`. `kMaxIndentTabs = 32` is plenty
(no real source has 32+ levels of nesting).

**Bug 3c — fold-cache race.** `RecalcFoldLevel` only sets a flag;
`UpdateFoldLevel` runs lazily on the next paint. PrepareTABs read
`GetFoldMask` while the cache was still stale from the previous
text-modified event → "every other Enter" symptom. Fixed by forcing
`m_fp.UpdateFoldLevel()` at the top of `PrepareTABs`.

**Bug 3d — OnCharAdded vs PrepareTABs whitespace mismatch.** The `}`
auto-aligner used `SetLineIndentation(line, matchIndent)` which honours
wxSTC's `UseTabs` setting (defaults to false → writes spaces). But
`PrepareTABs::applyIndent` hardcodes literal `\t`. Mixed tabs/spaces
got produced; `applyIndent` is no-shrink in CES so the spaces stuck
forever. Replaced with new helper `CopyLineLeadingWhitespace(stc, dst,
src)` that copies the **exact** leading whitespace (mix of tabs/spaces)
from match-line to current line. Both `}` aligner and `{` Allman
dedent route through it.

`reference_wxlonglong_printf.md` and `reference_fold_mask_precedence.md`
captured for future-reference; both are easy to repeat without notes.

### 4. Fold parser — CES brace-only

`Procedure BeforeStart(Cancel) { ...body... }` (hybrid VES keyword
header + CES brace body) gave double folds — `Procedure` opened
keyword fold on line 0, `{` opened brace fold on line 1, body got
2 tabs of indent instead of 1.

In CES, fold parser now ignores keyword folds entirely:

```cpp
const bool isCES = (ibCompileCode::GetCodeStyle() == CODE_CES);
for (const ibLexem& lex : lexems) {
    if (isCES) {
        if (lex.m_lexType == DELIMITER) {
            if (lex.m_numData == '{')      OpenFold(...,FoldBrace);
            else if (lex.m_numData == '}') CloseFold(...,FoldBrace);
        }
        continue;  // skip keyword folds entirely in CES
    }
    // ... VES keyword fold path unchanged ...
}
```

VES-only `End*` keywords are already filtered out by the `IsAllowedKey`
gate (next item) so they don't reach the fold parser anyway. CES uses
braces; VES uses keyword fences; no hybrid double-counting.

### 5. `IsAllowedKey` keyword gate

Single point of filtering for CES vs VES. Declared on
`ibTranslateCode` (translateCode.h) but body lives in `compileCode.cpp`
— the gate reads `gs_codeStyle` (compile-side state), and putting the
body there keeps the include chain one-way (compile depends on
translate, never reverse).

```cpp
// compileCode.cpp
bool ibTranslateCode::IsAllowedKey(int keywordId)
{
    if (gs_codeStyle == CODE_CES) {
        switch (keywordId) {
            case KEY_THEN:
            case KEY_DO:
            case KEY_ENDIF:
            case KEY_ENDDO:
            case KEY_ENDFUNCTION:
            case KEY_ENDPROCEDURE:
            case KEY_ENDTRY:
                return false;
        }
    }
    return true;
}
```

`ibTranslateCode::IsKeyWord` consults `IsAllowedKey` itself — VES-only
block-fence keywords return `wxNOT_FOUND` from `IsKeyWord` when CES
is active, so:

* **Lexer** — emits them as IDENTIFIER, not KEYWORD.
* **Syntax highlighter** — falls through to identifier-style colour.
* **Compile parser** — never sees `KEY_ENDPROCEDURE` etc; treats them
  as unknown identifiers and rejects accordingly.
* **Autocomplete dropdown** (`LoadSysKeyword` in codeEditorLoader.cpp)
  — calls `ibTranslateCode::IsAllowedKey(i)` directly when iterating
  the raw `s_listKeyWord` array. Same filter, single source of truth.

Switching styles via `SetCodeStyle` doesn't need a hash rebuild — the
gate reads fresh `gs_codeStyle` on every call.

### 6. Diagnostic log infrastructure

Three file-based logs landed (same pattern as the retired `lambda.log`
/ `intelli.log` / `fold.log` from previous session — file-append with
ISO timestamp, UTF-8 via vsnprintf to dodge wide-char surprises on
Windows):

* `scope.log` — block-scope compile trace. `ScopeLog` helpers in
  `compileCode.cpp` / `compileContext.cpp` / `procUnit.cpp` (each TU
  has its own anon-namespace copy, all writing to the same file). Hits
  CompileBlock entry/exit, AddVariable / FindVariable, CreateVariable,
  CompileLambdaExpression, CompileDeclaration, runtime CTX_BEGIN/END,
  Evaluate, ibCompileEval ctor, callable-var decision, GetCallFunction
  / PushCallFunction error paths.

* `fold.log` — fold parser trace. `ibCodeEditor::FoldLog` (static, body
  in codeEditor.cpp) called from inline ibFoldLevelParser methods
  (header) and from CalcFoldLevel / UpdateFoldLevel / Open / Close /
  MarkFold / ClearFoldLevel.

* `tab.log` — auto-indent trace. `ibCodeEditor::TabLog` (static, sibling
  of FoldLog) called from PrepareTABs / OnCharAdded / FormatSelection /
  IncreaseIndent / DecreaseIndent.

The two static methods on `ibCodeEditor` keep the helpers reachable
from both inline header methods (fold parser is template-style inline
in `codeEditor.h`) and `.cpp` callsites without anon-namespace ODR
landmines (one per TU producing different mangled symbols — a class of
pain that bit `datavgen` split in 2026-05-08).

### 7. printf variadic gotcha

`ibParamUnit::m_numIndex` / `m_numArray` are `wxLongLong_t` (8 bytes,
not `int`). Logging `slot=%d ... ctx=%p, target.m_numIndex, context`
silently broke variadic frame: `%d` consumed the lower 4 bytes
correctly, but the next arg `%p` read the upper 4 bytes (zero) instead
of `context`, so all `caller_ctx=00000000` logs were lying. Cast to
`(int)` at every site (or use `%lld`).

`reference_wxlonglong_printf.md` for future runs.

### Files touched (fourth pass)

* `src/engine/backend/compiler/compileCode.cpp` — CompileBlock entry
  precedence fix + `}` module-level error; callable-var parent walk
  + RETURN_BLOCK gate; ScopeLog helper + tracing across CompileBlock /
  CompileDeclaration / CompileLambdaExpression; `IsAllowedKey` body.
* `src/engine/backend/compiler/compileContext.cpp` — ScopeLog helper +
  tracing in AddVariable / CreateVariable / FindVariable.
* `src/engine/backend/compiler/compileContext.h` — `RETURN_BLOCK`
  enum kind.
* `src/engine/backend/compiler/procUnit.cpp` — ScopeLog in OPER_CTX_*
  cases + Evaluate / ibCompileEval ctor.
* `src/engine/backend/compiler/translateCode.{h,cpp}` —
  `IsAllowedKey` declaration + dispatch from `IsKeyWord`.
* `src/engine/frontend/win/editor/codeEditor/codeEditor.{h,cpp}` —
  fold parser CES brace-only; `GetFoldMask` precedence fix +
  `std::max(level-1, 0)`; `FoldLog` / `TabLog` static methods.
* `src/engine/frontend/win/editor/codeEditor/codeEditorLoader.cpp` —
  `applyIndent` clamp; PrepareTABs forces UpdateFoldLevel; OnCharAdded
  uses `CopyLineLeadingWhitespace` instead of `SetLineIndentation`;
  `LoadSysKeyword` filters via `IsAllowedKey`; TabLog tracing across
  PrepareTABs / OnCharAdded / FormatSelection / Increase/DecreaseIndent.

### Decisions to remember (fourth pass)

* **CES block scope is compile-only.** Block-scope vars share host
  frame slots (allocated via `CreateVariable` chain); `m_listVariable`
  membership in the child ctx is what gives them the visibility cliff
  at `}`. Runtime never sees a separate frame for `{ }`.

* **Block-scope vars are flagged `m_bContext=true`** so the emit path
  routes them through outer-frame slots. Distinguish from real Context
  bindings (Manager / ThisForm) by checking the ctx they live in:
  RETURN_BLOCK = block-scope artifact, RETURN_NONE on m_rootContext =
  real binding.

* **`IsAllowedKey` declared on translate-base, defined on compile-side.**
  Lexer / highlighter / autocomplete / parser all inherit the filter
  through `IsKeyWord`'s consultation of the gate. Include chain stays
  one-way (compile → translate); no hooks, no static-init ordering
  concerns. Switching styles is a plain field write.

* **Fold parser is style-exclusive.** CES uses brace folds only; VES
  uses keyword folds only. Hybrid sources (Procedure + braces) fold
  on whichever the active style mandates — the other gets filtered
  out (keyword in CES, brace in VES). No double-counting indent.

* **`m_fp.UpdateFoldLevel()` at PrepareTABs entry is mandatory.** The
  fold cache is a flag-set / lazy-paint pair; reading mask before
  paint draining catches the previous build, drops a tab on every
  other Enter. Force the rebuild upfront — cost is one
  RecalcFoldLevel per Enter, well within budget.

* **`OnCharAdded` brace-aligners must not use `SetLineIndentation`.**
  That API mixes in wxSTC's UseTabs setting and may swap tabs↔spaces
  vs what `applyIndent` (which hardcodes `\t`) writes. Use
  `CopyLineLeadingWhitespace` to copy the match-line's leading
  whitespace verbatim instead.

* **printf variadic frames care about argument size.** OES has
  `wxLongLong_t` fields named like ints (`m_numIndex`); logging them
  with `%d` corrupts the frame for every later arg. Cast to `(int)`
  at the call site or switch the format to `%lld`.

### Pending into next session (fourth pass)

Open items from earlier addendums still apply (lambda + AOT cache
verification, Accept(true) debug listener, watchdog `_exit(0)`,
ExpectBlock helpers, VES → CES converter, commit grouping). Add:

* **Diagnostic log strip pass.** scope/fold/tab logging needs the
  same removal cycle as lambda/intelli/fold did in the third pass —
  once block scope + CES tab handling stabilise.

* **`SetCodeStyle` doesn't fire any UI refresh.** Switching the
  syntax dropdown in codeRunner / module property in designer doesn't
  re-syntax-highlight the open editor — user has to type a char to
  retrigger styling. Cosmetic but surprising. Wire a `RefreshEditor`
  call from the property setter.

* **`CompileDeclaration` parent-walk dup-check.** Still rejects
  intra-module shadowing (`var x; { var x; }` errors with
  ERROR_DEF_VARIABLE). Per session-2026-04-30 §7 the check should be
  own-scope only; the loop in `compileCode.cpp:776-801` walks parent
  chain. Cosmetic in current usage but bites users who expect block
  scope to allow shadowing.
