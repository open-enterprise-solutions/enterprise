{
  "format": "OES-HELP-1.0",
  "schema_version": 1,
  "locale": "en",
  "entries": [
    {
      "id": "guide.syntaxHelper",
      "name_local": "About the Syntax Helper",
      "name_en": "About the Syntax Helper",
      "kind": "keyword",
      "category_keys": [
        "guide"
      ],
      "signature": "",
      "description": "The Syntax Helper exposes every language keyword, built-in function, primitive type, and metadata accessor available inside an OES configuration. It is read-only documentation that lives alongside the editor — it never modifies your configuration on its own.\n\nThree tabs cover three navigation patterns. Contents groups entries by category. Index lists every name alphabetically with prefix-search. Search runs a token query across names + signatures.\n\nUse the toolbar Back / Forward buttons to retrace your steps, Reload to pull in newly added help files without restarting, Sync to look up the identifier under the editor caret, and the Larger / Smaller text buttons to scale the description font. Press RawCtrl+F1 (Ctrl+F1 on Windows / Linux) in the editor to open the helper on the identifier under the cursor; RawCtrl+Alt+F1 toggles the pane.\n\nDrag-and-drop: pick an entry in the Index or Search list (or the Contents tree) and drag it into the editor — the matching syntax template inserts at the drop position. The template respects the active compile mode (CES braces vs VES End* terminators).",
      "syntax_block": "",
      "parameters": "",
      "return_descr": "",
      "example": "",
      "availability": "Designer, codeRunner, daemon, wenterprise-server",
      "see_also": [],
      "reviewed": true
    },
    {
      "id": "guide.linq",
      "name_local": "LINQ — collection queries",
      "name_en": "LINQ — collection queries",
      "kind": "keyword",
      "category_keys": [
        "common_lang",
        "linq"
      ],
      "signature": "from <item> in <source> [where ...] [orderby K1[, K2...] [ascending|descending]] [group ... by ... [into ...]] select ...",
      "description": "LINQ — language-integrated queries against in-memory collections. Two parallel surfaces compile to the same primitives.\n\n**Block syntax** (eager): `from <var> in <source>` opens the query; `where / var / orderby K1[, K2...] [asc|desc] / group X by K [into g] / join b in Y on a.K equals b.K / skip N / take N / distinct` clauses; closes with `select <expr>` or `select { F = expr, ... }`. Materialises to an Array immediately.\n\n**Chain syntax** (lazy): `arr.Where(λ).Select(λ).Distinct().Take(10).ToArray()`. 31 operations available — pipeline (Where / Select / Distinct / OrderBy / OrderByDescending / GroupBy / Join / Skip / Take / SkipWhile / TakeWhile / Reverse / Concat / Union / Intersect / Except / WhereIndexed / SelectIndexed), terminals (Count / ToArray / First / Last / Single / Any / ElementAt / Contains / SequenceEqual / Aggregate plus *OrDefault variants), aggregations (Sum / Min / Max / Average — with optional lambda selector).\n\nLambdas close over outer-scope locals: `var k = 5; arr.Where(Function(x) { return x > k; })`.\n\nLINQ keywords compile identically in CES and VES modes.",
      "syntax_block": "var q = from o in orders\n        where o.Total > 100\n        var doubled = o.Total * 2\n        orderby o.Warehouse, o.Date descending\n        select { N = o.N, V = doubled };",
      "parameters": "",
      "return_descr": "Array of selected rows (block syntax) or lazy iterable (chain syntax).",
      "example": "// chain syntax — lazy pipeline\nvar bigOnes = arr.Where(Function(x) { return x > 5; })\n                 .OrderByDescending(Function(x) { return x; })\n                 .Take(10)\n                 .ToArray();\n\n// aggregations with lambda selector\nvar total = arr.Sum();\nvar avgK  = arr.Average(Function(x) { return x.K; });\n\n// group-into continuation\nvar pairs = from p in people\n            group p.Name by p.Country into g\n            where g.Values.Count() > 1\n            select { Country = g.Key, Names = g.Values };",
      "availability": "Designer, codeRunner, daemon, wenterprise-server",
      "see_also": [
        "kw.From",
        "kw.Where",
        "kw.Select",
        "kw.OrderBy",
        "kw.Join",
        "kw.Group"
      ],
      "reviewed": true
    },
    {
      "id": "guide.lambda",
      "name_local": "Lambdas — functions as values (callback, handler)",
      "name_en": "Lambdas — functions as values (callback, handler)",
      "kind": "keyword",
      "category_keys": [
        "common_lang",
        "control_flow"
      ],
      "signature": "var <name> = Function([<Param1>, ...]) { <body> Return <value>; };",
      "description": "A FUNCTION CAN BE A VALUE. `Function(x) { return x * 2; }` written where a value is expected produces a callable that can be stored, passed, returned and called later. Same keyword as a declaration; what makes it a value is the position it stands in.\n\nWHAT THEY ARE FOR — three jobs, and finding yourself in one of them is how to know a lambda is wanted:\n\n1. A CALLBACK — work handed to somebody else to run: `arr.Where(Function(x) { return x > k; })`. The collection decides WHEN to call it and with what; you only say what to do with one element.\n\n2. AN EVENT HANDLER — behaviour attached to something that will happen later, without declaring a named procedure for it.\n\n3. A RULE PASSED AS AN ARGUMENT — a comparison, a filter, a projection given to a routine that would otherwise be fixed: `Function Apply(f, v) { return f(v); }`.\n\nTHEY REMEMBER WHERE THEY WERE WRITTEN (closure). A lambda reads the locals around it — of the procedure and of the module — and reads them AT CALL TIME, not when it was written:\n\n    var n = 1;\n    var f = Function(x) { return x + n; };\n    n = 100;\n    f(0)   // 100\n\nA parameter of the same name shadows the captured one, and the outer value is left untouched.\n\nCALLING ONE THAT LIVES IN A CONTAINER GOES THROUGH A VARIABLE: `s.f(1)` and `a[0](1)` do not compile, while `var g = s.f; g(1)` does.\n\n⚠ DO NOT REACH FOR A LAMBDA WHERE A CLAUSE SAYS IT SHORTER. Filtering, projecting, sorting and grouping a collection are written with KEYWORDS, and every lambda those would need is text you do not have to write:\n\n    from x in arr where x > limit select x                      // short\n    arr.Where(Function(x) { return x > limit; }).ToArray()      // the same thing, twice as long\n\nKeep lambdas for the three jobs above — a callback, a handler, a rule handed to somebody else. See guide.linq for the clause order.",
      "syntax_block": "var <name> = Function([<Param1>, ...]) { <body> Return <value>; };",
      "parameters": "",
      "return_descr": "",
      "example": "// a callback given to a pipeline\nvar limit = 3;\nvar big = arr.Where(Function(x) { return x > limit; }).ToArray();\n\n// a rule passed to a generic routine\nFunction Apply(f, v) { return f(v); }\nvar doubled = Apply(Function(x) { return x * 2; }, 21);\n\n// a function that answers with a function\nvar adder = Function(n) { return Function(x) { return x + n; }; };\nvar add10 = adder(10);\nadd10(7);   // 17",
      "availability": "Designer, codeRunner, daemon, wenterprise-server",
      "see_also": [
        "kw.Function",
        "guide.linq"
      ],
      "reviewed": true
    },
    {
      "id": "guide.declaredTypes",
      "name_local": "Declared types — say what a parameter or a variable takes",
      "name_en": "Declared types — say what a parameter or a variable takes",
      "kind": "keyword",
      "category_keys": [
        "common_lang"
      ],
      "signature": "Procedure <Name>(<Type> <param>[ = <default>], ...)    <Type> <name>;",
      "description": "THE TYPE STANDS BEFORE THE NAME: `Procedure OnClick(AnyControl element)`, `Array rows;`. It is optional - no type means anything - and it is WRITTEN FOR THE READER: whoever researches the code, a person or an assistant, sees what a routine takes without reading its body, and a type name that does not exist is a compile error rather than a surprise at run time. So declare the parameters of anything written for others to call.\n\nWHAT CAN BE DECLARED:\n\n- the primitives: `Number`, `String`, `Date`, `Boolean`;\n- a value class by its registered name: `Array`, `Structure`, `Table` (the value table)…;\n- a FAMILY - a filter that creates nothing, and lets a whole kind through, members added tomorrow included:\n    `Any` - anything, said rather than left unsaid\n    `AnyRef` - any reference (catalog, document, chart…)\n    `AnyObject` - any data object\n    `AnyManager` - any manager\n    `AnyControl` - any form control\n    `AnyValue` - any general-purpose value (array, structure, value table)\n    `AnyEnum` - any enumeration\n    `CatalogRef`, `DocumentRef`, `ChartOfAccountsRef`… - any reference OF ONE METATYPE, whichever object.\n\nEMPTY ALWAYS FITS. A parameter nobody passed or a reference not filled in is the empty value: the declaration says what the value IS when there is one, not that there is one.\n\nWHAT DOES NOT WORK YET (measured 2026-09-15):\n- `Val` together with a type - `Val Number n` does not compile. A typed parameter is written without `Val`.\n- one configuration object's reference, `CatalogRef.Goods item` - declare the family (`CatalogRef item`) and let the name say which catalog.\n- the editor's completion does not read a declared type yet; the declaration serves the reader and the compiler.\n\n`type_list` (the assistant's tool) answers which type names exist.",
      "syntax_block": "Procedure <Name>(<Type> <param>) { }",
      "parameters": "",
      "return_descr": "",
      "example": "// an event handler: any form control\nProcedure OnClick(AnyControl element)\n{\n    Message(element);\n}\n\n// any reference - whichever catalog, document or chart\nFunction Describe(AnyRef link)\n{\n    Return String(link);\n}\n\n// any document reference; a flag with its default\nProcedure Repost(DocumentRef doc, Boolean silent = False) Public\n{\n    If (Not silent) { Message(doc); }\n}\n\n// locals, declared the same way\nProcedure Collect()\n{\n    Array rows;\n    Table lines;\n    Structure totals;\n    Number count = 0;\n}",
      "availability": "Designer, codeRunner, daemon, wenterprise-server",
      "see_also": [
        "kw.Procedure",
        "kw.Function",
        "kw.Val",
        "fn.TypeOf"
      ],
      "reviewed": true
    },
    {
      "id": "guide.cached",
      "name_local": "Cached functions — an answer used again (settings, the policy for a period)",
      "name_en": "Cached functions — an answer used again (settings, the policy for a period)",
      "kind": "keyword",
      "category_keys": [
        "common_lang"
      ],
      "signature": "Function <Name>(<params>) [Public|Private|Protected] Cached { ... }",
      "description": "WHAT IS CACHED IS WHAT A FUNCTION RETURNS. `Cached` after the parameter list keeps the answer per combination of argument values: the first call with a combination runs the body, every later call with the same values gets the kept answer without entering the function. That is the whole point, which is why only a Function can be Cached - a Procedure returns nothing, and the modifier is refused there.\n\nWHEN TO WRITE IT - the same value is asked for again and again, and the answer depends on the arguments only:\n- the accounting policy for a period, asked for every line of every document;\n- settings and switches;\n- a rate for (currency, date);\n- anything worked out from metadata.\nA loop over ten thousand rows asking `InventoryMethod(period)` reads the register once per period, not ten thousand times. So whenever code reads the same setting or policy in more than one place, or inside a loop, the reading goes into a Cached function and every caller simply asks it.\n\nIT MAY RETURN ANYTHING, and the answer lives AS LONG AS ITS OWNER - there is nothing to clear and no timer:\n- a common module: the whole session;\n- an object module: while that object lives;\n- a form module: while the form is open.\nEvery caller with the same arguments gets the SAME value - a returned structure or table is one object for all of them.\n\nNOT FOR:\n- a function with side effects: the second call does not run;\n- an answer that depends on something besides the arguments (the current date, data written since): it is seen again only when the owner is gone - for a common module, the next session.\n\nArguments are compared BY VALUE: the number 1 and the string \"1\" are different keys. A call that raised is not kept.",
      "syntax_block": "Function <Name>(<params>) Public Cached {\n    Return <Value>;\n}",
      "parameters": "",
      "return_descr": "",
      "example": "// the policy for a period: read once per period, however often it is asked -\n// in a common module, for the whole session\nFunction InventoryMethod(Date period) Public Cached\n{\n    Return ReadPolicy(period);\n}\n\nFunction ReadPolicy(Date period) Private\n{\n    If (period < Date(2026, 1, 1)) { Return \"Average\"; }\n    Return \"FIFO\";\n}\n\n// the caller just asks, as often as it needs\nvar method = \"\";\nfor (i = 1 To 1000) {\n    method = InventoryMethod(Date(2026, 3, 1));   // the body ran once\n}",
      "availability": "Designer, codeRunner, daemon, wenterprise-server",
      "see_also": [
        "kw.Cached",
        "kw.Function",
        "guide.declaredTypes"
      ],
      "reviewed": true
    },
    {
      "id": "guide.basedOn",
      "name_local": "Entering on the basis — one object made from another (an invoice from an order)",
      "name_en": "Entering on the basis — one object made from another (an invoice from an order)",
      "kind": "keyword",
      "category_keys": [
        "common_lang"
      ],
      "signature": "Procedure Filling(Source, StandartProcessing)",
      "description": "ONE OBJECT ENTERED FROM ANOTHER - an invoice from an order, a payment from an invoice - is three pieces the platform already has. Nothing is written but the filling itself.\n\n1. WHAT MAY BE ENTERED FROM WHAT is a binding on the SOURCE: its `ListGeneration` property lists the kinds that can be made from it (the designer's property grid, or `metadata_bind` with `ListGeneration`). Empty: nothing is entered from it.\n\n2. THE PERSON presses the standard *Generate* command of the source's form. It offers exactly the kinds in that list, creates the chosen one and opens its form.\n\n3. THE NEW OBJECT FILLS ITSELF, in its own object module, in the handler `Filling(Source, StandartProcessing)`. `Source` is the reference it was entered from - the order. The same handler runs for a plain new object too, with `Source` empty - that is where its defaults go - so the first lines of the handler tell the two apart. Setting `StandartProcessing = False` REFUSES the entry: the object is not created and no form opens. Use it when this source cannot give such an object (an order already closed).\n\nFROM CODE the same filling is asked with `Fill`: `var doc = Documents.Invoice.CreateDocument(); doc.Fill(order); doc.Write();` - the new object's own `Filling` runs with that source. So the rule lives in ONE place, whether a person or a routine enters the object.\n\nWHAT TO CARRY OVER is craft, not mechanism - the pattern `entered-from` (pattern_read): attributes of the same name carry over; the rate, the currency of a till and the number are worked out afresh for the new document; lines are filled from what is STILL OUTSTANDING on the source (`document-chains`), not copied from its lines as written.",
      "syntax_block": "Procedure Filling(Source, StandartProcessing) {\n    If (Not ValueIsFilled(Source)) { Return; }\n    <fill from Source>\n}",
      "parameters": "Source - the reference the object is entered from; empty for a plain new object. StandartProcessing - True on entry; set it to False to refuse creating the object.",
      "return_descr": "",
      "example": "// the object module of the document being ENTERED (here: a receipt entered from an order)\nProcedure Filling(Source, StandartProcessing)\n{\n    If (Not ValueIsFilled(Source)) {\n        Return;                        // a plain new document: defaults only\n    }\n    // the header by name; the lines from what the order still has open\n    Counterparty = Source.Counterparty;\n    Warehouse = Source.Warehouse;\n    foreach (line In Source.Goods) {\n        var row = Goods.Add();\n        row.Item = line.Item;\n        row.Quantity = line.Quantity;\n    }\n}\n\n// the same, asked from code\nvar doc = Documents.GoodsReceipt.CreateDocument();\ndoc.Fill(order);\ndoc.Write();",
      "availability": "Designer, codeRunner, daemon, wenterprise-server",
      "see_also": [
        "kw.Procedure",
        "fn.ValueIsFilled",
        "guide.cached"
      ],
      "reviewed": true
    },
    {
      "id": "guide.ownedCatalogs",
      "name_local": "Subordinate catalogs — items that live with an owner (units of an item, contracts of a counterparty)",
      "name_en": "Subordinate catalogs — items that live with an owner (units of an item, contracts of a counterparty)",
      "kind": "keyword",
      "category_keys": [
        "common_lang"
      ],
      "signature": "ListOwner = <owner catalog>    item.Owner",
      "description": "A CATALOG WHOSE ITEMS BELONG TO AN ITEM OF ANOTHER - the units of measure of a product, its characteristics, the contracts or bank accounts of a counterparty - is declared in METADATA, not written in code. The subordinate catalog's `ListOwner` names the catalogs that own it: the designer's property grid, `metadata_bind` with `ListOwner`, or `ListOwner` among `metadata_create`'s properties. Like any catalog it may be hierarchical as well.\n\nWHAT IT GIVES:\n- every item and folder gets the predefined attribute `Owner`, typed by that list - a reference to the owner's item (several owner catalogs make it a composite type);\n- `Owner` is REQUIRED: an item without one is refused on write;\n- `Owner` is an ordinary column - `SELECT ... FROM Catalog.Units WHERE Owner = &Item`, and in LINQ `From u In Data.Catalogs.Units Where u.Owner = item`.\n\nWHEN: the thing makes sense only with its owner - units, packings, characteristics, contracts, a counterparty's bank accounts. What many owners share (a currency, a warehouse) is not subordinate.\n\nHOW IT BEHAVES FOR THE PERSON. A subordinate list is shown for ONE owner, and a choice on a document line offers only the items of the line's owner - a contract of the document's counterparty, a unit of the line's product. That is the point of subordination, and it is the PLATFORM's to do, by the owner - not each configuration's. So when building a configuration, LOOK whether the list and the choice are narrowed; where they are not, that is a requirement to raise with the platform's owner, not a workaround to write into every form. A QUERY is another matter: it reads what it is asked, so a query over one owner's items filters by `Owner` itself.",
      "syntax_block": "var <item> = Catalogs.<Subordinate>.CreateElement();\n<item>.Owner = <ownerRef>;\n<item>.Write();",
      "parameters": "",
      "return_descr": "",
      "example": "// the catalog Units has ListOwner = Goods (set in metadata, not here)\nvar unit = Catalogs.Units.CreateElement();\nunit.Owner = item;                 // required\nunit.Description = \"box of 12\";\nunit.Write();\n\n// the units of one product\nvar units = From u In Data.Catalogs.Units Where u.Owner = item Select u;",
      "availability": "Designer, codeRunner, daemon, wenterprise-server",
      "see_also": [
        "guide.linq",
        "guide.basedOn"
      ],
      "reviewed": true
    },
    {
      "id": "guide.textFiles",
      "name_local": "Files — reading and writing text, and bytes as they are (TextReader, TextWriter, BinaryData, TextEncoding)",
      "name_en": "Files — reading and writing text, and bytes as they are (TextReader, TextWriter, BinaryData, TextEncoding)",
      "kind": "keyword",
      "category_keys": [
        "common_lang"
      ],
      "signature": "New TextReader(<path> [, <TextEncoding>])    New TextWriter(<path> [, <TextEncoding> [, <append>]])",
      "description": "THE CONTENT OF A FILE, FROM A SCRIPT. `File` answers whether a file exists and how large it is; these read and write what is in it - an exchange file of another system, sales out of a cash register, a price list - on every platform, with no COM object in between.\n\nTEXT, LINE BY LINE - `TextReader`. `ReadLine()` hands over the next line WITHOUT its line break (CRLF and LF alike); `Read()` hands over everything that is left as one text; `Close()` lets the file go. THE END OF THE FILE IS `Undefined`, NOT AN EMPTY STRING: an empty line is a line, and a loop written against `\"\"` stops at the first blank line of the file - or, written the other way round, never stops.\n\nBY NUMBER - `LineCount()` is how many lines the file has, `GetLine(n)` is line n counted FROM ONE: the very lines `ReadLine()` hands over, cut the same way, and neither moves where `ReadLine()` is. A line break at the very end of the file ENDS the last line rather than opening an empty one after it: a file of `\"a\"` + LF is ONE line, where `StrLineCount`, which counts a text, says two. No such line is `Undefined`, as the end of the file is for `ReadLine()`.\n\nWRITING - `TextWriter`. `Write(text)` writes the text as it is, `WriteLine(text)` adds ONE line break - LF, the same on every platform, so a file written here reads the same there. The third constructor argument, `True`, APPENDS to the file instead of replacing it. A writer nobody closed still leaves its text on disk, but close it: the file stays open until then.\n\nTHE ENCODING is said with the `TextEncoding` enumeration - `UTF8` (when left out), `UTF16`, `ANSI` and `OEM` (the code pages of the machine the platform runs on, asked of the system), `System`. A byte-order mark at the head of a UTF-8 or UTF-16 file is read as a mark and never handed over as text; a UTF-16 mark also says which byte comes first. On writing, UTF-16 gets its mark and UTF-8 does not. Bytes that do not spell text in the encoding given are an ERROR, not a text with holes in it - so is a file that cannot be opened, and the message names the file.\n\nBYTES - `BinaryData`. `Read(path)` takes the file's content whole, `Write(path)` puts the content into a file, `Size()` is the number of bytes. A string handed to the CONSTRUCTOR still means base64, not a path - which is why reading a file is a method.\n\nThe whole file is decoded when it is opened: these are for exchange files, not for logs of gigabytes.",
      "syntax_block": "var <reader> = New TextReader(<path> [, TextEncoding.<X>]);\n<line> = <reader>.ReadLine();   // Undefined at the end\n<rest> = <reader>.Read();\n<count> = <reader>.LineCount();  <line> = <reader>.GetLine(<n>);   // from 1, Undefined past the last\n<reader>.Close();\n\nvar <writer> = New TextWriter(<path> [, TextEncoding.<X> [, <append>]]);\n<writer>.Write(<text>);  <writer>.WriteLine(<text>);\n<writer>.Close();\n\nvar <bytes> = New BinaryData();\n<bytes>.Read(<path>);  <bytes>.Write(<path>);  <bytes>.Size();",
      "parameters": "",
      "return_descr": "",
      "example": "var reader = New TextReader(\"C:\\in\\sales.csv\", TextEncoding.UTF8);\nvar line = reader.ReadLine();\nwhile (line <> Undefined) {          // Undefined, not \"\": an empty line is a line\n    if (line <> \"\") {\n        var cut = Find(line, \";\");\n        var code = Left(line, cut - 1);   // the first field\n    }\n    line = reader.ReadLine();\n}\nreader.Close();\n\nvar writer = New TextWriter(GetTempDir() + \"out.csv\", TextEncoding.UTF8);\nwriter.WriteLine(\"code;name;qty\");\nwriter.Close();\n\nvar bytes = New BinaryData();\nbytes.Read(\"C:\\in\\logo.png\");\nMessage(bytes.Size());\nbytes.Write(\"C:\\out\\logo.png\");",
      "availability": "Designer, codeRunner, daemon, wenterprise-server",
      "see_also": [],
      "reviewed": false
    }
  ]
}
